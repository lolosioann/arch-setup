# General Settings Configuration

# Dotfiles repository (REQUIRED)
DOTFILES_REPO="https://github.com/lolosioann/dotfiles.git"

# Dotfiles branch (optional, defaults to 'main')
DOTFILES_BRANCH="main"

# Local dotfiles directory (optional, defaults to ~/.dotfiles)
DOTFILES_DIR="$HOME/.dotfiles"

# Git configuration (optional - set if not already configured)
GIT_USERNAME="lolosioann"
GIT_EMAIL="lolosioann@gmail.com"

# Shell preferences (optional)
DEFAULT_SHELL="/bin/zsh"

# Additional settings
# INSTALL_NVIDIA_DRIVERS=false
# INSTALL_AMD_DRIVERS=false
# ENABLE_MULTILIB=true
